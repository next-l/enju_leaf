<%=
events = @events
RiCal.Calendar do
  events.each do |event|
    event do
      description event.title
      dtstart     event.start_at
      dtend       event.end_at
      location    event.library.address
    end
  end
end.to_s
-%>
