<%= form_for(@manifestation) do |f| %>
  <% if @manifestation.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@manifestation.errors.count, "error") %> prohibited this resource from being saved:</h2>
    <ul>
    <% @manifestation.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :original_title %><br />
    <%= f.text_area :original_title %>
  </div>
  <div class="field">
    <%= f.label :isbn %><br />
    <%= f.text_field :isbn %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
