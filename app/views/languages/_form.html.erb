<%= simple_form_for(@language, :validate => true) do |f| %>
  <%= f.error_notification %>

  <div class="field">
    <%= f.label :native_name %><br />
    <%= f.text_field :native_name %>
  </div>

  <div class="field">
    <%= f.label :name %>:<br />
    <%= @language.name %>
  </div>

  <div class="field">
    <%= f.label :display_name %><br />
    <%= f.text_area :display_name, :class => 'span12' %>
  </div>

  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note, :class => 'span12' %>
  </div>

  <div class="actions">
    <%= f.submit :class => 'btn' %>
  </div>
<% end %>
