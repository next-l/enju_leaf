<p>
  <%= t('page.no_record_found') -%>
  <% if false %>
    <% unless !@search_engines.nil? && @search_engines.empty? %>
      <%= t('page.you_can_search_following_database') -%>
      <%= render 'manifestations/search_engine' -%>
    <% end %>
  <% end %>
</p>
