title,isbn,item_identifier,call_number,created_at
<%- @items.each do |item| -%>
"<%=h item.manifestation.original_title.gsub(/"/, '""') if item.manifestation -%>",<%=h item.manifestation.isbn %>,<%=h item.item_identifier -%>,<%=h call_numberformat(item) -%>,<%=h item.created_at %><%= "\n" -%>
<%- end -%>
