<div id="content_detail">
<h1 class="title"><%= t('page.listing', :model => t('activerecord.models.create')) -%></h1>
<div id="content_list">
  <h2 class="resource_title">
    <%= link_to h(@work.original_title), @work -%>
  </h2>

<table class="index">
  <tr>
    <th id="position"></th>
    <th><%= t('activerecord.models.patron') -%></th>
    <th><%= t('page.created_at') -%></th>
    <th><%= t('page.updated_at') -%></th>
  </tr>

<%- for create in @creates -%>
  <tr class="line<%= cycle("0", "1") -%>">
    <td>
      <%- if can? :destroy, create -%>
        <%= create.first? ? image_tag('cross.png') : link_to(image_tag('arrow_up.png', :size => '16x16', :alt => t('page.asc')), {:action => 'update', :position => (create.position > 1 ? create.position - 1 : 1), :id => create.id, :work_id => @work.id}, :method => :put) -%>
        <%= create.last? ? image_tag('cross.png') : link_to(image_tag('arrow_down.png', :size => '16x16', :alt => t('page.desc')), {:action => 'update', :position => (create.position + 1), :id => create.id, :work_id => @work.id}, :method => :put) -%>
      <%- end-%>
    </td>
    <td><%= link_to h(create.patron.full_name), create.patron -%></td>
    <td><%= l(create.created_at) -%></td>
    <td><%= l(create.updated_at) -%></td>
    <%- if can? :destroy, create -%>
      <td><%= link_to t('page.show'), work_create_path(@work, create) -%></td>
      <td><%= link_to t('page.destroy'), work_create_path(@work, create), :confirm => t('page.are_you_sure'), :method => :delete -%></td>
    <%- end -%>
    </tr>
<%- end -%>
</table>

<%= will_paginate(@creates) -%>
</div>
</div>

<div id="submenu">
  <ul>
    <%- if can? :create, Create -%>
      <li><%= link_to t('page.new', :model => t('activerecord.models.create')), new_work_create_path(@work) -%></li>
    <%- end -%>
  </ul>
</div>
