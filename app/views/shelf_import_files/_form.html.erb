<%= form_with(model: shelf_import_file) do |form| %>
  <% if shelf_import_file.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shelf_import_file.errors.count, "error") %> prohibited this shelf_import_file from being saved:</h2>

      <ul>
        <% shelf_import_file.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
