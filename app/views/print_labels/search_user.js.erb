$('#result').html(
    "<%= escape_javascript(render :partial => 'user_list') %>"
);
var selector = $("div#print_users").find("input");
selector.each(function() {
 var value = $(this).val();
 $('a#user_' + value).hide();
});
