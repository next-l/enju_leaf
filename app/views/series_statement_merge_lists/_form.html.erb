<%= form_for(@series_statement_merge_list) do |f| %>
  <%= error_messages(@series_statement_merge_list) %>
  <div class="field">
    <%= f.label :title -%><br />
    <%= f.text_field :title -%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
